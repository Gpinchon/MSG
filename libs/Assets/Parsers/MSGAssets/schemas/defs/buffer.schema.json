{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "$id": "buffer.schema.json",
  "title": "MSGAssets-buffer",
  "type": "object",
  "definitions": {
    "buffer": {
      "title": "Buffer",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "the object's name, important because it can be used to reference it"
        },
        "uri": {
          "$ref": "external.schema.json#/definitions/uri"
        }
      },
      "required": [
        "uri"
      ]
    },
    "bufferView": {
      "title": "BufferView",
      "type": "object",
      "properties": {
        "uri": {
          "$ref": "external.schema.json#/definitions/uri"
        },
        "name": {
          "$comment": "required if uri is not present",
          "description": "the object's name, important because it can be used to reference it",
          "type": "string"
        },
        "size": {
          "$comment": "required if uri is not present",
          "description": "the size of this buffer view in bytes, only necessary if this bufferView uses a buffer a its source",
          "type": "number",
          "minimum": 0
        },
        "offset": {
          "$comment": "required if uri is not present",
          "description": "the offset of this buffer view inside the buffer in bytes, only necessary if this bufferView uses a buffer a its source",
          "type": "number",
          "minimum": 0
        },
        "buffer": {
          "$comment": "required if uri is not present",
          "description": "the index of the buffer into the buffers list",
          "type": "number",
          "minimum": 0
        }
      },
      "if": {
        "not": {
          "required": [
            "uri"
          ]
        }
      },
      "then": {
        "required": [
          "name",
          "buffer",
          "size",
          "offset"
        ]
      }
    }
  }
}