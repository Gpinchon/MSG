{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "$id": "image.schema.json",
  "title": "MSGAssets-image",
  "type": "object",
  "definitions": {
    "image": {
      "title": "Image",
      "type": "object",
      "description": "an image if a set of pixels that can be 3 dimensional, it's untyped as the typing comes from Texture",
      "properties": {
        "uri": {
          "$ref": "external.schema.json#/definitions/uri"
        },
        "name": {
          "$comment": "required if uri is not present",
          "description": "the image's name",
          "type": "string"
        },
        "pixelFormat": {
          "$comment": "required if uri is not present",
          "description": "the format used to interpret the image",
          "$ref": "pixelFormat.schema.json#/definitions/pixelFormat"
        },
        "size": {
          "$comment": "required if uri is not present",
          "description": "the size of the image",
          "$ref": "vec.schema.json#/definitions/vec3"
        },
        "bufferView": {
          "$comment": "required if uri is not present",
          "description": "an index into the bufferviews list",
          "type": "number",
          "minimum": 0
        }
      },
      "if": {
        "not": {
          "required": [
            "uri"
          ]
        }
      },
      "then": {
        "required": [
          "name",
          "pixelFormat",
          "size",
          "bufferView"
        ]
      }
    }
  }
}